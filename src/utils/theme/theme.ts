export const getColor = (name: string) => {
  if (name === "cyan") return "#10d4d2";
  else if (name === "red") return "#fb0b12";
  else if (name === "green") return "#019707";
  else if (name === "orange") return "#d68102";
  else return "#349eff";
};